<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Label Your URLs</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-T3c6CoIi6uLrA9TneNEoa7RxnatzjcDSCmG1MXxSR1GAsXEV/Dwwykc2MPK8M2HN" crossorigin="anonymous">
    <link rel="stylesheet" href="/styles/define_url.css">
</head>
<body>
    <%- include("partials/header.ejs") %>

    <!-- Title -->
    <div class="px-4 py-5 pb-0 my-5 text-center">
        <h1 class="display-5 fw-bold text-body-emphasis">Your Shortened URLs are Ready!</h1>
        <div class="col-lg-6 mx-auto">
          <p class="lead mb-4">Here are your shortened URL links to track the source of your traffic!</p>
          <!-- <div class="d-grid gap-2 d-sm-flex justify-content-sm-center">
            <button type="button" class="btn btn-primary btn-lg px-4 gap-3">Primary button</button>
            <button type="button" class="btn btn-outline-secondary btn-lg px-4">Secondary</button>
          </div> -->
        </div>
    </div>

    <% if (locals.url_instance) { %>
        <div class="container col-xl-10 col-xxl-8 px-4 py-5">
            <form action="/define_url" method="POST" class="p-4 p-md-5 border rounded-3 bg-body-tertiary" >
                <div class="row" style="height: 50px;">
                    <div class="col-lg-auto">
                        <h2>Original URL:</h2>
                    </div>
                    <div class="col-lg-4">
                        <textarea disabled class="form-control col-7 " aria-label="With textarea" name="user_url" style="height: 40px;"><%= user_url %> </textarea>
                    </div>
                </div>
                <br>
                <div class="input-group">
                    <span class="input-group-text style="  style="width: 45px;">No</span>
                    <span class="input-group-text col-4 text-wrap ">Shortened URL</span>
                    <span class="input-group-text col-7 text-wrap ">Location</span>
                </div>
                
                <% let count = 1; %>
                <% for (let i=0; i< url_instance.length; i = i+3) {  %>
                    <div class="input-group">
                        <span class="input-group-text style="  style="width: 45px;"><%= count %></span>
                        <span class="input-group-text col-4 text-wrap "><%= url_instance[i] %></span>
                        <span class="input-group-text col-7 text-wrap "><%= url_instance[i+1] %></span>
                        <!-- <textarea class="form-control col-7 " aria-label="With textarea" name="label-<%= count %>" disabled style="margin: auto;"><%= url_instance[i+1] %></textarea> -->
                    </div>
                    <% count++ %>
                <% } %>
                <br>
                <!-- <div class="row justify-content-center">
                    <button class="w-25 btn btn-primary rounded-pill px-3 " type="submit">Save</button>
                </div>    -->
            </form>
        </div>
        <% } %>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js" integrity="sha384-C6RzsynM9kWDrMNeT87bh95OGNyZPhcTNXj1NW7RuBCsyN/o0jlpcV8Qyq46cDfL" crossorigin="anonymous"></script>

</body>
</html>