<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Visit History</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-T3c6CoIi6uLrA9TneNEoa7RxnatzjcDSCmG1MXxSR1GAsXEV/Dwwykc2MPK8M2HN" crossorigin="anonymous">
    <link rel="stylesheet" href="/styles/define_url.css">
</head>
<body>
    <%- include("partials/header.ejs") %>

    <!-- Title -->
    <div class="px-4 py-5 pb-0 my-5 text-center">
        <h1 class="display-5 fw-bold text-body-emphasis">Track Your Shortened URLs</h1>
        <div class="col-lg-6 mx-auto">
          <p class="lead mb-4">Here you can track the traffic to your link based on your shortened URLs!</p>
          <!-- <div class="d-grid gap-2 d-sm-flex justify-content-sm-center">
            <button type="button" class="btn btn-primary btn-lg px-4 gap-3">Primary button</button>
            <button type="button" class="btn btn-outline-secondary btn-lg px-4">Secondary</button>
          </div> -->
        </div>
    </div>

    <!-- Table -->
    <div class="container-xl">

        <% if (locals.db) { %>
            <% for (let i=0; i<db.length; i++) { %>
                <div class="row" style="height: 50px;">
                    <div class="col-lg-auto">
                        <h2>Original URL:</h2>
                    </div>
                    <div class="col-lg-4">
                        <textarea disabled class="form-control col-7 " aria-label="With textarea" name="user_url" style="height: 40px;"> <%= db[i][0] %> </textarea>
                    </div>
                </div>

                

                <div class="table-responsive small">
                    <table class="table table-striped table-sm">
                        <thead>
                            <tr>
                            <th scope="col">#</th>
                            <th scope="col">UUID</th>
                            <th scope="col">Location</th>
                            <th scope="col">No. of Visits</th>
                            </tr>
                        </thead>
                        <% let count = 0 %>
                        <% for (let j=0; j<db[i][2].length; j = j+3) { %>
                            <tbody>
                                <tr>
                                <td> <%= count %></td>
                                <td><%= db[i][2][j] %></td>
                                <td><%= db[i][2][j+1] %></td>
                                <td><%= db[i][2][j+2] %></td>
                                </tr>
                            </tbody>
                            <% count += 1 %>
                        <% } %> 
                    </table>
                </div>
                <br><br>
            <% } %>
        <% } %>

    </div>

 

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js" integrity="sha384-C6RzsynM9kWDrMNeT87bh95OGNyZPhcTNXj1NW7RuBCsyN/o0jlpcV8Qyq46cDfL" crossorigin="anonymous"></script>

</body>
</html>